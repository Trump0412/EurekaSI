# Isolated geometry environment; do not install into an active worker prefix.
# Versions used by the real CPU Qwen/PEFT integration acceptance.
torch==2.7.1
transformers==5.3.0
peft==0.18.1
# The existing geometry-matrix environment also supplies Pillow, NumPy,
# safetensors and the checked-out VGGT implementation. This is a constraint
# record, not a complete bare-machine installer or a claim of VERL support.
